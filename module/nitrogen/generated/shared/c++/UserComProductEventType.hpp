///
/// UserComProductEventType.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::usercom {

  /**
   * An enum which can be represented as a JavaScript enum (UserComProductEventType).
   */
  enum class UserComProductEventType {
    ADDTOCART      SWIFT_NAME(addtocart) = 0,
    PURCHASE      SWIFT_NAME(purchase) = 1,
    LIKING      SWIFT_NAME(liking) = 2,
    ADDTOOBSERVATION      SWIFT_NAME(addtoobservation) = 3,
    ORDER      SWIFT_NAME(order) = 4,
    RESERVATION      SWIFT_NAME(reservation) = 5,
    RETURN      SWIFT_NAME(return) = 6,
    VIEW      SWIFT_NAME(view) = 7,
    CLICK      SWIFT_NAME(click) = 8,
    DETAIL      SWIFT_NAME(detail) = 9,
    ADD      SWIFT_NAME(add) = 10,
    REMOVE      SWIFT_NAME(remove) = 11,
    CHECKOUT      SWIFT_NAME(checkout) = 12,
    CHECKOUTOPTION      SWIFT_NAME(checkoutoption) = 13,
    REFUND      SWIFT_NAME(refund) = 14,
    PROMOCLICK      SWIFT_NAME(promoclick) = 15,
  } CLOSED_ENUM;

} // namespace margelo::nitro::usercom

namespace margelo::nitro {

  // C++ UserComProductEventType <> JS UserComProductEventType (enum)
  template <>
  struct JSIConverter<margelo::nitro::usercom::UserComProductEventType> final {
    static inline margelo::nitro::usercom::UserComProductEventType fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      int enumValue = JSIConverter<int>::fromJSI(runtime, arg);
      return static_cast<margelo::nitro::usercom::UserComProductEventType>(enumValue);
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::usercom::UserComProductEventType arg) {
      int enumValue = static_cast<int>(arg);
      return JSIConverter<int>::toJSI(runtime, enumValue);
    }
    static inline bool canConvert(jsi::Runtime&, const jsi::Value& value) {
      if (!value.isNumber()) {
        return false;
      }
      double number = value.getNumber();
      int integer = static_cast<int>(number);
      if (number != integer) {
        // The integer is not the same value as the double - we truncated floating points.
        // Enums are all integers, so the input floating point number is obviously invalid.
        return false;
      }
      // Check if we are within the bounds of the enum.
      return integer >= 0 && integer <= 15;
    }
  };

} // namespace margelo::nitro
